class Items:

    #wheels = 2
    
    def __init__(self, id, title, status ):
        self.id = id
        self.title = title
        self.status = status
        # Start in first gear.
        #self.gear = 1;

    #def change_gear(self, gear):
    list = []
    list = { 'id': self.id , 'title': self.title, 'status': self.status }