before_install:
- openssl aes-256-cbc -K $encrypted_1e3732aec487_key -iv $encrypted_1e3732aec487_iv -in Secrets.enc -out Secrets -d

import:
- Secrets
script:

- echo "This is Daves travis.yml"
- echo "-----------------------------------x"


- FLASK_APP=todo_app/app
- FLASK_ENV=development


- docker build --target test --tag my-test-image .

- docker run -p 5000:5000 my-test-image
